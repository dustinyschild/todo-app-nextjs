blocks = [1, 2, 3, 4, 5]

// 
const multiplyBy2 = blocks.map((num) => {
    return num * 2;
})
// output => 2, 4, 6, 8, 10

// functions the same as: 

loopMultiplyBy2 = []
for (let i = 0; i <= blocks.length; i++) {
    loopMultiplyBy2.push(blocks[i] * 2)
}

// returns all the 'true' values
blocks.filter((block) => {
    if (block === 'wheat') {
        
    }
})

